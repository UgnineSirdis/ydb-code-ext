{
  "name": "ydbcode",
  "displayName": "YDB Code",
  "description": "Extension for YDB developers that helps to do everyday job",
  "version": "1.0.0",
  "license": "MIT",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/UgnineSirdis/ydb-code-ext"
  },
  "engines": {
    "vscode": "^1.105.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:ydb/apps/ydbd",
    "workspaceContains:ydb/apps/ydb",
    "workspaceContains:ydb/public/tools/local_ydb"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ydbcode.createLocalYdb",
        "title": "Build and run new local YDB in specified folder",
        "icon": "$(add)"
      },
      {
        "command": "ydbcode.stopLocalYdb",
        "title": "Stop running local YDB",
        "icon": "$(notebook-stop)"
      },
      {
        "command": "ydbcode.refreshLocalYdb",
        "title": "Refresh local YDB list",
        "icon": "$(refresh)"
      },
      {
        "command": "ydbcode.localYdbInfo",
        "title": "Local YDB connection info",
        "icon": "$(info)"
      },
      {
        "command": "ydbcode.buildStartLocalYdb",
        "title": "Build and restart",
        "icon": "$(debug-rerun)"
      },
      {
        "command": "ydbcode.startLocalYdb",
        "title": "Restart without build",
        "icon": "$(notebook-execute)"
      },
      {
        "command": "ydbcode.deleteLocalYdb",
        "title": "Stop and delete",
        "icon": "$(trash)"
      },
      {
        "command": "ydbcode.editConfigLocalYdb",
        "title": "Edit Config",
        "icon": "$(wrench)"
      },
      {
        "command": "ydbcode.openLogsLocalYdb",
        "title": "Open Logs",
        "icon": "$(output)"
      }
    ],
    "views": {
      "debug": [
        {
          "id": "local-ydb",
          "name": "Local YDB",
          "when": "ydbcode.enabled"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "ydbcode.createLocalYdb",
          "when": "view == local-ydb && ydbcode.enabled",
          "group": "navigation@1"
        },
        {
          "command": "ydbcode.stopLocalYdb",
          "when": "view == local-ydb && ydbcode.enabled",
          "group": "navigation@2"
        },
        {
          "command": "ydbcode.refreshLocalYdb",
          "when": "view == local-ydb && ydbcode.enabled",
          "group": "navigation@3"
        },
        {
          "command": "ydbcode.localYdbInfo",
          "when": "view == local-ydb && ydbcode.enabled",
          "group": "navigation@4"
        }
      ],
      "view/item/context": [
        {
          "command": "ydbcode.buildStartLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "inline@1"
        },
        {
          "command": "ydbcode.startLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "inline@2"
        },
        {
          "command": "ydbcode.deleteLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "inline@3"
        },
        {
          "command": "ydbcode.editConfigLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "inline@4"
        },
        {
          "command": "ydbcode.openLogsLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "inline@5"
        },
        {
          "command": "ydbcode.buildStartLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "actions"
        },
        {
          "command": "ydbcode.startLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "actions"
        },
        {
          "command": "ydbcode.stopLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "actions"
        },
        {
          "command": "ydbcode.deleteLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "actions"
        },
        {
          "command": "ydbcode.editConfigLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "actions"
        },
        {
          "command": "ydbcode.openLogsLocalYdb",
          "when": "view == local-ydb && viewItem == localYdb && ydbcode.enabled",
          "group": "actions"
        }
      ]
    },
    "configuration": {
      "title": "YDB",
      "properties": {
        "ydb.localYdbPorts.monPort": {
          "type": "number",
          "description": "Monitoring port for local YDB. Default is computed from your username."
        },
        "ydb.localYdbPorts.grpcPort": {
          "type": "number",
          "description": "gRPC port for local YDB. Default is computed from your username."
        },
        "ydb.localYdbPorts.grpcTlsPort": {
          "type": "number",
          "description": "gRPC TLS port for local YDB. Default is computed from your username."
        },
        "ydb.localYdbPorts.icPort": {
          "type": "number",
          "description": "Interconnect port for local YDB. Default is computed from your username."
        },
        "ydb.localYdbPorts.grpcExtPort": {
          "type": "number",
          "description": "External gRPC port for local YDB. Default is computed from your username."
        },
        "ydb.localYdbPorts.publicHttpPort": {
          "type": "number",
          "description": "Public HTTP port for local YDB. Default is computed from your username."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.105.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.48.1",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
